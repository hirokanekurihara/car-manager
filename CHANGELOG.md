# 変更履歴（Changelog）

このアプリは v1.0.0 からバージョン番号による管理を開始しました。
それ以前の開発過程（Firebase移行、保険・タイヤ管理、レシートOCRなど）は
GitHubの「Commits」履歴（上部の時計アイコン）で確認できます。

## [1.8.0] - 2026-08-20
### 変更
- チケット使用時、パック名に「NOJ」を含むチケットに限り、自動登録されるメンテ完了記録の
  カテゴリを「洗車・コーティング」ではなく「ボディメンテナンス」として記録するように変更
  （残り枚数の表示は作業内容欄にそのまま維持されるため、実質的にボディメンテナンス内で
  残量が確認できるようになる）
- 過去にチケット使用で記録された「洗車・コーティング」のうち、作業内容に「NOJ」を含む記録を
  自動的に「ボディメンテナンス」へ一括移行する処理を追加（ログイン時に自動実行。既に移行済みの
  記録は対象条件に一致しなくなるため、二重に処理されることはない）
- カテゴリマスタに「ボディメンテナンス」が存在しない場合は、自動で追加するようにした

## [1.7.0] - 2026-08-19
### 変更
- メンテナンスタブの完了済み一覧を「実施日（新しい順）」でソート表示するように変更
- 完了済みのメンテ記録も編集できるように対応
  - 誤って「予約中」に巻き戻せないよう、完了済み記録の編集時は「更新する」ボタンのみ表示
- 作業カテゴリを、マスタからの選択に加えてテキストで直接入力できるように変更
- 店舗マスタに「デフォルト担当者」を設定可能に。メンテ予約時に店舗を選択すると、
  担当者欄が未入力の場合に限り自動入力される機能を追加（手入力済みの内容は上書きしない）
### 修正
- メンテ予約モーダル内の備考欄（id="mMemo"）が重複していたバグを修正
- Googleカレンダー追加ボタンで、車両詳細を開いていないと車両名が空欄になる不具合を修正

## [1.6.1] - 2026-08-18
### 追加
- メンテナンス予約モーダルに「🗓️ この内容をGoogleカレンダーに追加」ボタンを新設
  - 予約日・予約時間・カテゴリ・店舗・担当者・備考の入力内容から、Googleカレンダーの
    予定作成画面をワンタップで開けるようにした
  - 予約時間が未入力の場合は終日予定、入力済みの場合は1時間枠の予定として生成
  - Google Calendar APIやOAuthスコープの追加は行わず、URL生成のみで完結する方式を
    採用（既存のFirebase認証設定・Firestore保存処理には一切影響しない設計）


## [1.6.0] - 2026-08-17
### 追加
- 車両の並び替え機能を追加
  - 設定タブに「🚘 車両の表示順」セクションを新設し、▲▼ボタンで並び替え可能に
  - この並び順は、車両タブのカード一覧と、給油・メンテタブの「対象車両」プルダウンの両方に反映される
  - 既存の車両データには、初回読み込み時に自動でsortOrderを付与するマイグレーション処理を実装
    （カテゴリ・店舗マスタ・給油店舗マスタと同一方式）
  - 新規車両登録時は、既存の最大sortOrder+1を自動採番

## [1.5.0] - 2026-08-17
### 追加
- 設定タブに「💾 バックアップと復元」セクションを新設
- バックアップ機能：全データ（設定・カテゴリ・店舗マスタ・給油店舗マスタ・
  車両とその全サブコレクション）をJSON形式で一括エクスポート
  - `schema: 'car-manager-backup-v1'` / `exportedAt` / `appVersion` / `uid` を
    含む正規フォーマットで出力し、誤ったファイルの復元を防止
  - Firestoreのtimestamp系フィールドはISO文字列に変換して保存
- 復元機能：バックアップJSONから「完全置換方式」で復元
  - schemaフィールドによる正規ファイル検証
  - 2段階の確認ダイアログによる誤操作防止（「全削除されます」→「元に戻せません、
    本当によろしいですか」）
  - 既存データを全コレクション・全サブコレクションから削除してからバックアップ
    内容を書き戻すことで、マージによるデータ重複・不整合（二重投薬的事故）を
    原理的に防止
  - Firestore `writeBatch` の500件制限に対応するため、`BATCH_CHUNK_SIZE = 400`
    で削除・書き込みの両方をチャンク分割処理
  - 復元時、`createdAt`/`updatedAt`は`serverTimestamp()`で再生成。日付文字列
    フィールド（`date`・`shaken`・`expiry`等）はそのまま文字列として復元

### 変更
- アプリ内部バージョンを `v1.5.0` に更新（ヘッダー・設定タブの表示に反映）

## [v1.4.3] - 2026-08-16
### 追加・変更
- **給油記録の編集機能を追加**: 給油履歴カードの「✏️」ボタンから、日付・走行距離・給油量・金額・満タンフラグ・給油店舗を編集できるようになりました。
- **燃費再計算ロジックの強化**: 編集保存時、「編集中の自分自身を除外した、入力ODOより小さい直近の記録」を基準に燃費を自動再計算するようにしました。
- **車両走行距離（ODO）更新の安全化**: 給油記録保存時、入力したODOが現在の車両登録値より大きい場合のみcars.odoを更新するよう変更。過去記録の編集による走行距離の誤巻き戻りを防止しました。
- **アイコン設定の追加**: `icon.png`をapple-touch-icon・faviconとして`<head>`に設定しました（相対パス統一）。

## [1.4.2] - 2026-08-16

### Added
- 車両基本情報の燃料種別に「e-POWER」を追加（ガソリン / ハイブリッド / e-POWER / 軽油 / EV / PHEV）。日産の公式表記に合わせて大文字・ハイフン付きで統一。

## [1.4.1] - 2026-08-16

### Added
- **年間計画タブ**：計画イベントの繰り返し設定に「3ヶ月ごと」を追加（none / 3month / 6month / 1year / 2year）。既存の登録済みデータには影響なし。

## [1.4.0] - 2026-08-16

### Added
- **年間計画タブ（📅）を新設**：車両詳細モーダルに追加。ボディメンテナンス・6ヶ月点検・タイヤ交換・ラストムーンメンテナンス等の周期的な計画を、独立コレクション `annualPlans` として登録・編集・削除できる。上部に今月起点の12ヶ月サマリーバー（ドット表示）、次回イベントまでの残日数カード、時系列の予定・過去リストを表示。繰り返し設定（6ヶ月・1年・2年）と予算・メモも記録可能。
- **車検・保険満了日の自動連携表示**：車両基本情報・保険タブにすでに登録済みの車検満了日／任意保険満了日／自賠責満了日を、二重入力せずに年間計画タイムラインへ自動合成表示（Firestoreには書き込まない表示専用の仕組み）。
- **店舗マスタ（メンテ用・給油用）に名前・電話番号のインライン編集を追加**：入力欄を直接書き換えてフォーカスを外すと自動保存される。

### Changed
- 車両詳細モーダルのタブが6タブ構成（基本・保険・タイヤ・チケット・計画・分析）に変更。

## [1.3.0] - 2026-08-15

### Changed
- **給油店舗をメンテ店舗マスタから完全分離**：給油記録専用の「⛽ 給油店舗マスタ」（`fuelShops`）を新設。設定タブに追加・削除・並び替え可能な管理セクションを追加し、給油フォームはマスタ選択＋自由入力の併用方式に変更。レシートOCRの店舗名推測も、まず給油店舗マスタとの一致を優先し、次点でENEOS/出光/コスモ等のブランドキーワードを検出する方式に更新。
- **メンテ作業チェックリストの表示条件を限定**：カテゴリ名に「車検整備」または「定期点検」が含まれる場合のみ、実施した作業のチェックリストを表示するよう変更（部分一致判定のためカテゴリ名編集にも追従）。新規登録・編集の両方で正しく表示制御される。
- **保険料を3年契約の年ごと入力＋3年合計表示に変更**：`insurance.annualFee`（単一値）を廃止し、`annualFee1`/`annualFee2`/`annualFee3`（各年の保険料）に変更。保険タブに1年目・2年目・3年目の金額と3年合計をカード表示。分析タブの年間維持費計算では「3年合計÷3」の年換算額を使用（旧データの`annualFee`にはフォールバック対応）。

## [1.2.0] - 2026-08-15

### Added
- **タブ並び替え機能**：設定タブに「📱 タブの表示順」セクションを追加。▲▼ボタンで並び替えた順序は `settings/appSettings.tabOrder` に保存され、iPhone・iPad・Macの全デバイスで共有される。「初期の並び順に戻す」ボタンも実装。並び替え操作中に表示中のタブが強制的に切り替わらないよう配慮した。
- **給油記録に店舗名フィールドを追加**：店舗マスタからのプルダウン選択と自由入力欄を併用可能にし、レシートOCR時は①店舗マスタとの部分一致照合→②ENEOS/出光/コスモ等の主要ブランドキーワード検出、の2段階で店舗名を自動推測してフォームへ反映する。
- **給油単価のリアルタイム自動計算**：給油量・金額の入力に連動してフォーム内に単価（円/L）を即時表示。保存時に `unitPrice` フィールドとしてFirestoreへ記録し、履歴一覧にも `@¥xxx/L` 形式で表示する（旧データはその場で計算してフォールバック表示）。
- **分析タブ（📊）を新規実装**：車両詳細モーダルに追加。Chart.js（v4系固定・CDN）を導入し、直近1年の維持費合計カード、月別費用の積み上げ棒グラフ（給油・メンテ・チケット、直近12ヶ月）、年間費用の内訳ドーナツグラフ（給油・メンテ・チケット・保険料）、燃費推移の折れ線グラフ（直近20件の満タン法燃費）を表示する。
- **`insurance.annualFee`（年間保険料）フィールドを追加**：保険編集モーダルに入力欄を追加し、分析タブの年間維持費計算に反映されるようにした。
- **iPhoneホーム画面追加時（standaloneモード）の画面最適化**：`viewport-fit=cover` とCSSの `env(safe-area-inset-*)` を用いてノッチ・ホームインジケーター領域を確保。ヘッダー上部のパディングはstandaloneモード時のみ調整し、通常のSafari表示には影響しない。

### Fixed
- メンテナンスタブの「予約中・進行中」「完了済み」サブタブ切り替えボタンのイベントハンドラを復元。
- `getRedirectResult` のエラーハンドリングで `auth/null-user` を正常系として無視するよう修正。

## [v1.1.0] - 2026-08-15
### Added
- 設定タブの「作業カテゴリ」「店舗マスタ」に▲▼ボタンによる並び替え機能を追加
- チケットパックの購入店舗を、店舗マスタからのプルダウン選択（＋自由入力との併用）に対応
- メンテナンス完了時の入力フォームに「実施した作業」チェックボックスを追加
  （作業カテゴリマスタと連動して自動生成されるため、カテゴリを追加するだけで
  チェック項目も増える。二重管理が不要）
- メンテナンス完了時専用の「備考」欄を新設し、予約時の備考とは別に記録できるように変更
### Fixed
- 過去バージョンで登録済みのカテゴリ・店舗データに並び順情報がない場合でも、
  重複登録されずに既存データへ自動的に並び順が設定されるよう移行処理を追加

## [v1.0.0] - 2026-08-14
### Added
- 燃費計算を「満タン法」に変更し、精度を向上
  （前回・今回とも満タン給油だった場合のみ算出）
- 給油タブに燃費サマリーカード（平均・最新・最良・最悪）を追加
- 車両詳細に「🎫チケット」タブを新設
  - ボディメンテナンス等の回数券（チケットパック）を車両ごとに登録可能に
  - 「1枚使用する」ボタンで残り枚数が自動減算され、使用履歴が記録される
  - チケット使用時、メンテナンスの「完了済み」履歴にも自動で記録される
- ヘッダーおよび設定タブにアプリのバージョン番号を表示
